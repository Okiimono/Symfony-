{% extends 'base.html.twig' %}

{% block title %}
	Accueil
{% endblock %}

{% block body %}
    <div class="card col-md-12" xmlns="http://www.w3.org/1999/html">
		<div class="card-header">
			<h3>
				<i class="fas fa-home"></i>
				 {% trans %}default.index.acceuil_boutique{% endtrans %}
			</h3>
		</div>
		<div class="card-body">
			<div class="card clickable" onclick="location = '{{ path("app_boutique_index") }}'">

                {# Pour fabriquer l'URL d'une ressource du répertoire public, on utilise le helper 'asset' #}
				<img src="{{ asset("images/epicerie.jpg") }}" class="card-img-top" alt="Logo Boutique"/>

				<div class="card-body">
					<p class="card-text">
						<i>{% trans %}boutique.rayon.vide{% endtrans %}</i>
					</p>
				</div>
			</div>
		</div>
	</div>
{% endblock %}
